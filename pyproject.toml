[project]
name = "pdf-ocr"
version = "0.1.0"
description = "PDF OCR tools using Docling and Marker"
requires-python = ">=3.12"
dependencies = [
    "docling>=2.82.0",
    "marker-pdf[full]>=1.10.2",
    "opencv-contrib-python>=4.10.0",
    "pdf2image>=1.17.0",
]
